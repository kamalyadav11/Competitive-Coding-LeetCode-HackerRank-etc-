function deleteCharacter(str) {
	for(let i = 0; i < str.length; i++) {
		switch(str) {
			case "a":
			 delete a;
			case "e":
			 delete "e";
			case "i":
			 delete "i";
			case "o":
			 delete "o";  
		}
	}
	return str;
}

console.log(deleteCharacter("Battle of the Vowels"));